import { AccountAuthenticator, AnyRawTransaction, Network } from "@aptos-labs/ts-sdk";
import { type Config, createClient } from "./generated/client";
export interface GasStationClientOptions extends Config {
    network?: Network;
    env?: "prod" | "staging";
    apiKey?: string;
}
export interface SignAndSubmitTransactionBody {
    transaction: AnyRawTransaction;
    senderAuthenticator: AccountAuthenticator;
    secondaryAuthenticators?: AccountAuthenticator[];
    recaptchaToken?: string;
}
export declare class GasStationClient {
    readonly client: ReturnType<typeof createClient>;
    constructor(options: GasStationClientOptions);
    signAndSubmitTransaction(body: SignAndSubmitTransactionBody): Promise<import("./generated").SignAndSubmitTransactionResponse>;
}
//# sourceMappingURL=GasStationClient.d.ts.map